{
  "name": "your-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER -c green,blue \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
